#include "monty.h"
/**
 * noOperation - Does nothing; a placeholder function.
 * @stack_head: Pointer to the stack's head
 * @line_number: line_number
 * Return: no return
 */
void f_nope(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
